# This rule is not used by the project saas-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the platform.tibrahim.dev.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: saas-operator
    app.kubernetes.io/managed-by: kustomize
  name: platform-dataplatform-editor-role
rules:
- apiGroups:
  - platform.tibrahim.dev
  resources:
  - dataplatforms
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - platform.tibrahim.dev
  resources:
  - dataplatforms/status
  verbs:
  - get
